{
  "name": "claudemeter",
  "displayName": "Claudemeter - Claude AI Usage Monitor",
  "description": "Monitor your Claude Code usage proactively in real time, with full limit information. No more 'Surprise! You've hit your Claude Code weekly limit and it resets in 3 days you lucky, lucky person!'. Tracks session, weekly, and token limits across all Claude plans.",
  "version": "2.3.5",
  "publisher": "hypersec",
  "author": {
    "name": "HyperI",
    "url": "https://hyperi.io"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hyperi-io/claudemeter.git"
  },
  "bugs": {
    "url": "https://github.com/hyperi-io/claudemeter/issues"
  },
  "homepage": "https://github.com/hyperi-io/claudemeter#readme",
  "license": "MIT",
  "icon": "assets/claudemeter-logo-icon-128.png",
  "galleryBanner": {
    "color": "#1a1a2e",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.110.0"
  },
  "extensionKind": [
    "ui"
  ],
  "keywords": [
    "claude",
    "claude ai",
    "claude code",
    "anthropic",
    "token",
    "tokens",
    "usage",
    "monitor",
    "tracker",
    "limit",
    "limits",
    "rate limit",
    "weekly limit",
    "session limit",
    "context window",
    "quota",
    "free plan",
    "pro plan",
    "max plan",
    "team plan",
    "sonnet",
    "opus",
    "status bar",
    "ai assistant",
    "llm",
    "api usage",
    "reset time",
    "spending cap",
    "credits"
  ],
  "categories": [
    "Visualization",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "package": "node esbuild.js --production && rm -f dist/*.map && vsce package",
    "publish": "node esbuild.js --production && rm -f dist/*.map && vsce publish",
    "clear-session": "node scripts/clear-session.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "contributes": {
    "commands": [
      {
        "command": "claudemeter.fetchNow",
        "title": "Fetch Claude Usage Now",
        "category": "Claudemeter"
      },
      {
        "command": "claudemeter.openSettings",
        "title": "Open Claude Settings Page",
        "category": "Claudemeter"
      },
      {
        "command": "claudemeter.startNewSession",
        "title": "Start New Claude Code Session",
        "category": "Claudemeter"
      },
      {
        "command": "claudemeter.showDebug",
        "title": "Show Debug Output",
        "category": "Claudemeter"
      },
      {
        "command": "claudemeter.resetConnection",
        "title": "Reset Browser Connection (Legacy)",
        "category": "Claudemeter"
      },
      {
        "command": "claudemeter.clearSession",
        "title": "Clear Session (Re-login)",
        "category": "Claudemeter"
      },
      {
        "command": "claudemeter.openBrowser",
        "title": "Login to Claude.ai",
        "category": "Claudemeter"
      },
      {
        "command": "claudemeter.resyncAccount",
        "title": "Resync Account (after /login switch)",
        "category": "Claudemeter"
      },
      {
        "command": "claudemeter.dumpState",
        "title": "Dump State (for bug reports)",
        "category": "Claudemeter"
      }
    ],
    "configuration": {
      "title": "Claudemeter",
      "properties": {
        "claudemeter.fetchOnStartup": {
          "type": "boolean",
          "default": true,
          "description": "Automatically fetch usage data when VS Code starts"
        },
        "claudemeter.autoRefreshMinutes": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "maximum": 60,
          "description": "Auto-refresh interval in minutes for fetching Claude.ai usage data (1-60 minutes)"
        },
        "claudemeter.localRefreshSeconds": {
          "type": "number",
          "default": 10,
          "minimum": 5,
          "maximum": 60,
          "description": "Local token refresh interval in seconds (5-60 seconds). Controls how often local Claude Code token data is polled from JSONL files. Set higher to reduce CPU usage."
        },
        "claudemeter.statusBar.displayMode": {
          "type": "string",
          "enum": [
            "default",
            "minimal",
            "compact"
          ],
          "default": "default",
          "enumDescriptions": [
            "Full display: Claude Se 13% ⏱ 11:59  Wk 11% ⏱ Fri 11  Tk 36%",
            "Percentages only: Claude Se 13%  Wk 11%  Tk 36%",
            "Single compact panel: Claude S-13% Wk-11% Tk-36%"
          ],
          "markdownDescription": "Status bar display mode:\n- **default**: Full display with reset times (separate panels)\n- **minimal**: Percentages only (separate panels)\n- **compact**: All metrics in a single panel"
        },
        "claudemeter.statusBar.showSonnet": {
          "type": "boolean",
          "default": false,
          "description": "Show Sonnet weekly usage in status bar (default/minimal modes only)"
        },
        "claudemeter.statusBar.showOpus": {
          "type": "boolean",
          "default": false,
          "description": "Show Opus weekly usage in status bar (Max plans only, default/minimal modes)"
        },
        "claudemeter.statusBar.showCredits": {
          "type": "boolean",
          "default": false,
          "description": "Show extra usage (spending cap) in status bar (default/minimal modes only)"
        },
        "claudemeter.statusBar.showServiceStatus": {
          "type": "boolean",
          "default": true,
          "description": "Show Claude service status indicator. When enabled, displays a warning/error icon if Claude services are degraded or experiencing an outage."
        },
        "claudemeter.statusBar.timeFormat": {
          "type": "string",
          "enum": [
            "12hour",
            "24hour",
            "countdown"
          ],
          "default": "countdown",
          "enumDescriptions": [
            "12-hour format with AM/PM (e.g., 2:30 PM)",
            "24-hour format (e.g., 14:30)",
            "Countdown timer (e.g., 2h 15m)"
          ],
          "markdownDescription": "How to display reset times in the status bar"
        },
        "claudemeter.statusBar.alignment": {
          "type": "string",
          "enum": [
            "left",
            "right"
          ],
          "default": "right",
          "description": "Status bar alignment (left or right side). Requires window reload to take effect."
        },
        "claudemeter.statusBar.priority": {
          "type": "number",
          "default": 100,
          "minimum": 0,
          "maximum": 10000,
          "description": "Status bar priority (higher values position items closer to the centre). Requires window reload to take effect."
        },
        "claudemeter.statusBar.usageFormat": {
          "type": "string",
          "enum": [
            "percent",
            "barLight",
            "barSolid",
            "barSquare",
            "barCircle"
          ],
          "default": "barCircle",
          "enumDescriptions": [
            "Percentage (e.g., 60%)",
            "Light blocks (e.g., ▓▓▓░░)",
            "Solid blocks (e.g., ███░░)",
            "Squares (e.g., ■■■□□)",
            "Circles (e.g., ●●●○○)"
          ],
          "markdownDescription": "How to display usage values in the status bar"
        },
        "claudemeter.statusBar.tokensDisplay": {
          "type": "string",
          "enum": [
            "bar",
            "value",
            "extended",
            "limit",
            "count"
          ],
          "default": "limit",
          "enumDescriptions": [
            "Progress bar / percent only (e.g. Tk ●●○○○)",
            "Progress bar + current consumption (e.g. Tk ●●○○○ 518k)",
            "Progress bar + current/max (e.g. Tk ●●○○○ 518k/1m)",
            "Progress bar + max only when extended beyond 200K (e.g. Tk ●●○○○ 1m on a 1M session; just the bar on a 200K session) — default",
            "Token count only, no bar (e.g. Tk 518k/1m)"
          ],
          "markdownDescription": "How to display the **Tk** (token usage) item in the status bar:\n\n- **bar**: progress bar / percent only — `Tk ●●○○○`\n- **value**: bar + current consumption — `Tk ●●○○○ 518k`\n- **extended**: bar + current/max — `Tk ●●○○○ 518k/1m` (this was the pre-2.3.3 `both` default)\n- **limit** _(default)_: bar + max, shown only when greater than the 200K standard context window — `Tk ●●○○○ 1m` on a 1M session; just the bar on a 200K session\n- **count**: token count only, no bar — `Tk 518k/1m`\n\nWhen the context window limit is inferred rather than authoritative, the max is suppressed to avoid misrepresenting an uncertain value.\n\n_Backward compatibility:_ the legacy `both` value is auto-migrated to `extended` on startup so any existing configuration keeps its previous look._"
        },
        "claudemeter.thresholds.warning": {
          "type": "number",
          "default": 80,
          "minimum": 1,
          "maximum": 100,
          "description": "Usage percentage to show warning (yellow) indicator"
        },
        "claudemeter.thresholds.error": {
          "type": "number",
          "default": 90,
          "minimum": 1,
          "maximum": 100,
          "description": "Usage percentage to show error (red) indicator"
        },
        "claudemeter.thresholds.tokens.warning": {
          "type": "number",
          "default": 65,
          "minimum": 1,
          "maximum": 100,
          "description": "Token usage warning threshold"
        },
        "claudemeter.debug": {
          "type": "boolean",
          "default": false,
          "description": "Enable debug logging to output channel (for troubleshooting)"
        },
        "claudemeter.happyHour.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show a 'happy hour' icon on the Claude status-bar panel during Anthropic's off-peak window (when their peak-throttling policy is not active).\n\nPeak window defaults to Mon-Fri 05:00-23:00 America/Los_Angeles, per Anthropic's 2026-03-26 announcement. Configurable via `claudemeter.happyHour.peakWindow`.\n\nAnthropic throttling is driven by LA-local peak load — this icon appears outside that window regardless of your own timezone."
        },
        "claudemeter.happyHour.icon": {
          "type": "string",
          "enum": [
            "sparkle",
            "watch",
            "zap",
            "star",
            "beer",
            "cocktail",
            "wine",
            "champagne",
            "martini",
            "coffee",
            "moon",
            "sparkles",
            "palm",
            "party",
            "custom"
          ],
          "default": "sparkle",
          "enumDescriptions": [
            "Sparkle (monochrome codicon, default) — subtle, unobtrusive",
            "Watch (monochrome codicon) — subtle, time-themed",
            "Zap (monochrome codicon) — lightning bolt",
            "Star (monochrome codicon)",
            "🍺 Beer",
            "🍹 Cocktail",
            "🍷 Wine",
            "🥂 Champagne",
            "🍸 Martini",
            "☕ Coffee (teetotal)",
            "🌙 Moon (late-night coder)",
            "✨ Sparkles",
            "🌴 Palm tree",
            "🎉 Party",
            "Custom — set claudemeter.happyHour.customIcon"
          ],
          "markdownDescription": "Icon shown during happy hour. The first four options are monochrome VS Code codicons that inherit the status-bar text colour (subtle, unobtrusive). The remainder are full-colour emoji. Set to `custom` and provide `claudemeter.happyHour.customIcon` to use any glyph (emoji or `$(codicon-name)` syntax)."
        },
        "claudemeter.happyHour.customIcon": {
          "type": "string",
          "default": "",
          "markdownDescription": "Custom emoji for happy hour (used only when `claudemeter.happyHour.icon` is `custom`)."
        },
        "claudemeter.happyHour.peakWindow": {
          "type": "object",
          "default": {
            "days": [
              1,
              2,
              3,
              4,
              5
            ],
            "start": "05:00",
            "end": "23:00",
            "tz": "America/Los_Angeles"
          },
          "properties": {
            "days": {
              "type": "array",
              "items": {
                "type": "integer",
                "minimum": 0,
                "maximum": 6
              },
              "description": "Days of week where peak applies. 0=Sunday, 1=Monday, ..., 6=Saturday."
            },
            "start": {
              "type": "string",
              "pattern": "^\\d{2}:\\d{2}$",
              "description": "Peak window start time in HH:MM 24-hour format, in `tz`."
            },
            "end": {
              "type": "string",
              "pattern": "^\\d{2}:\\d{2}$",
              "description": "Peak window end time in HH:MM 24-hour format, in `tz`."
            },
            "tz": {
              "type": "string",
              "description": "IANA timezone identifier (e.g. America/Los_Angeles)."
            }
          },
          "markdownDescription": "Anthropic's announced peak-throttling window. Happy hour shows OUTSIDE this window.\n\nDefaults to `{ days: [1,2,3,4,5], start: \"05:00\", end: \"23:00\", tz: \"America/Los_Angeles\" }` per Anthropic's 2026-03-26 announcement. Override here if the policy changes.\n\nMalformed fields fall back to the defaults individually (e.g. a bad `tz` still keeps your custom `start`/`end`)."
        },
        "claudemeter.tokenLimit": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 2000000,
          "description": "Context window token limit override (0 = auto-detect from model and plan). Set manually to force a specific limit."
        },
        "claudemeter.tokenOnlyMode": {
          "type": "boolean",
          "default": false,
          "description": "Token-only mode: Only track Claude Code tokens, skip Claude.ai web usage scraping entirely"
        },
        "claudemeter.useLegacyScraper": {
          "type": "boolean",
          "default": false,
          "description": "Use legacy browser-based scraper instead of streamlined HTTP fetching. The default HTTP method calls undocumented Claude.ai API endpoints that could change without notice. Enable this fallback if the HTTP method stops working due to API changes. Requires a Chromium-based browser (Chrome, Edge, Brave, etc.)."
        },
        "claudemeter.debugLogFile": {
          "type": "string",
          "default": "",
          "description": "Path to debug log file. Auto-populated on first run. Supports ~ for home directory."
        },
        "claudemeter.debugLogMaxSizeKB": {
          "type": "number",
          "default": 256,
          "minimum": 64,
          "maximum": 2048,
          "description": "Maximum debug log file size in KB. Oldest entries are trimmed when exceeded (64-2048 KB, default 256 KB)"
        }
      }
    }
  },
  "dependencies": {
    "puppeteer-core": "^24.40.0"
  },
  "overrides": {
    "undici": ">=7.24.5",
    "flatted": ">=3.4.2",
    "picomatch": ">=2.3.2",
    "brace-expansion": ">=1.1.13",
    "basic-ftp": ">=5.2.2",
    "vite": ">=8.0.5",
    "lodash": ">=4.18.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.28.0",
    "eslint": "^10.1.0",
    "vitest": "^4.1.1"
  }
}
